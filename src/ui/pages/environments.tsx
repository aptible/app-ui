import { AppSidebarLayout } from "../layouts";
import { EnvironmentList } from "../shared";

export const EnvironmentsPage = () => {
  return (
    <AppSidebarLayout>
      <EnvironmentList />
    </AppSidebarLayout>
  );
};
