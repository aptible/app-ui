import { MenuWrappedPage } from "../layouts";
import { EndpointsByOrg } from "../shared";

export const EndpointsPage = () => {
  return (
    <MenuWrappedPage>
      <EndpointsByOrg />
    </MenuWrappedPage>
  );
};
