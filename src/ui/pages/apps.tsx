import { ListingPageLayout } from '../layouts';

import { AppList } from '../shared';

export const AppsPage = () => {
  return (
    <ListingPageLayout>
      <AppList />
    </ListingPageLayout>
  );
};
