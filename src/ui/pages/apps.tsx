import { MenuWrappedPage } from "../layouts/menu-wrapper";
import { AppList } from "../shared";

export const AppsPage = () => {
  return (
    <MenuWrappedPage>
      <AppList />
    </MenuWrappedPage>
  );
};
