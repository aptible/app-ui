import { ListingPageLayout } from '../layouts';

import { AppList } from './app';

export const AppsPage = () => {
  return (
    <ListingPageLayout>
      <AppList />
    </ListingPageLayout>
  );
};
