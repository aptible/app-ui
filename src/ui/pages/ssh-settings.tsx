import { Button } from '../button';

export const SSHSettingsPage = () => {
  return (
    <div className="p-4">
      <h1>SSH Settings</h1>
      <Button>Add another SSH key</Button>
    </div>
  );
};
