const { borderRadius, borderWidth } = require('./borders');
const { boxShadow } = require('./box-shadow');
const { colors, borderColors } = require('./colors');
const { fontFamily } = require('./font-family');
const { fontSize } = require('./font-size');
const { fontWeight } = require('./font-weight');
const { height, maxHeight } = require('./height');
const { letterSpacing } = require('./letter-spacing');
const { lineHeight } = require('./line-height');
const { margin } = require('./margin');
const { padding } = require('./padding');
const { screens } = require('./screens');
const { maxWidth, minWidth, width } = require('./width');
const { willChange } = require('./will-change');
const { zIndex } = require('./z-index');

module.exports = {
  borderColors,
  borderRadius,
  borderWidth,
  boxShadow,
  colors,
  fontFamily,
  fontSize,
  fontWeight,
  height,
  letterSpacing,
  lineHeight,
  margin,
  maxHeight,
  maxWidth,
  minWidth,
  padding,
  screens,
  width,
  willChange,
  zIndex,
};
